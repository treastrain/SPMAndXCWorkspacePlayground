    import XCTest
    @testable import MyAppPackage

    final class MyAppPackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(MyAppPackage().text, "Hello, World!")
        }
    }
